export enum UserRole{
    USER,
    STAFF,
    ADMIN=99
}
export enum UserStatus{
    DEACTIVE,
    ACTIVE

}

export enum TagUser{
    ADMIN,
    AGENCY_VIP,
    AGENCY_SUPPERLIRER,
    AGENCY,
}

export enum Rank{
    DEFAULT,
    SILVER,
    GOLD,
    DIAMOND
}